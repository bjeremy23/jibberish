# Core dependencies for Jibberish
click>=8.0.0
openai>=1.0.0
psutil>=7.0.0  # Required for job_control_command.py (known working version)

# Optional dependencies - For Azure OpenAI support
# Note: Package availability depends on your Python version 
# Uncomment ONE of the following options:

# Option 1: azure-identity + azure-ai-openai (if available)
# azure-identity>=1.12.0
# azure-ai-openai>=1.0.0  

# Option 2: azure-identity + azure-openai (alternative package name)
# azure-identity>=1.12.0
# azure-openai>=1.0.0

# Option 3: Azure support via OpenAI package
# openai[azure]>=1.0.0
